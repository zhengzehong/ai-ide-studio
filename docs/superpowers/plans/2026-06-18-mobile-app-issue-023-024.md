# Mobile App Issue 023 024 Implementation Plan

**For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) superpowers:executing-plans implement plan task-by-task. Steps use checkbox (`- [ ]`) syntax tracking.

**Goal:** Fix mobile chat sends while disconnected and Android back-button listener cleanup without changing existing mobile chat/list features.

**Architecture:** Keep the fixes local to the mobile shell. Chat send gets a connection guard at both UI and store boundaries; Android back handling keeps one native listener and reads latest navigation state from refs.

**Tech Stack:** React 19, Zustand 5, TypeScript 6, Vitest, Capacitor App plugin.

---

### Task 1: Guard Mobile Chat Sending When Disconnected

**Files:**
- Modify: `mobile/src/stores/chat.store.ts`
- Modify: `mobile/src/pages/ChatPage.tsx`
- Test: `tests/unit/mobile-chat-store.test.ts`

- [x] **Step 1: Write failing store test**

Add a test that sets `wsClient.connected` to false, calls `sendPrompt`, and verifies no local human message, no pending streaming bubble, and no running state are created.

- [x] **Step 2: Run focused test and verify RED**

Run: `npx vitest run tests/unit/mobile-chat-store.test.ts`

Expected: the new disconnected-send test fails because `sendPrompt` currently appends local optimistic state even when `wsClient.send` silently drops the message.

- [x] **Step 3: Implement connection guard**

Add `sendError` to the mobile chat store state. In `sendPrompt`, check `wsClient.connected` before `wsClient.send`; when disconnected, set `sendError` to `连接已断开，消息未发送` and return before mutating messages or running state. Clear `sendError` on successful send path and on session entry.

- [x] **Step 4: Disable input while disconnected**

In `ChatPage`, read `connected/status` from `useConnectionStore`. Pass `disabled={hasBlockingInteraction || !connected}` to `ChatInput` and use a disconnected placeholder for connecting/failed states.

- [x] **Step 5: Run focused test and verify GREEN**

Run: `npx vitest run tests/unit/mobile-chat-store.test.ts`

Expected: all tests in the file pass.

### Task 2: Make Android Back Listener Cleanup Robust

**Files:**
- Modify: `mobile/src/components/AndroidBackHandler.tsx`
- Test: `tests/unit/mobile-android-back-handler.test.ts`

- [x] **Step 1: Write failing listener lifecycle test**

Mock delayed native listener registration. Register the Android back listener, run cleanup before the Promise resolves, resolve with a handle, and verify `handle.remove()` is called.

- [x] **Step 2: Run focused test and verify RED**

Run: `npx vitest run tests/unit/mobile-android-back-handler.test.ts`

Expected: the new lifecycle test fails because cleanup currently calls `removeListener?.()` before the handle exists.

- [x] **Step 3: Implement one-listener ref-based handler**

Use refs for latest `pathname`, `serverUrl`, and `navigate`. Register the native `backButton` listener once on Android. Add a `disposed` flag so a late-resolving handle is removed immediately after cleanup.

- [x] **Step 4: Run focused test and verify GREEN**

Run: `npx vitest run tests/unit/mobile-android-back-handler.test.ts`

Expected: all tests in the file pass.

### Task 3: Verify And Commit

**Files:**
- Verify all files changed in Tasks 1-2 plus this plan.

- [x] **Step 1: Run targeted tests**

Run:
- `npx vitest run tests/unit/mobile-chat-store.test.ts tests/unit/mobile-android-back-handler.test.ts`

- [x] **Step 2: Run broader checks**

Run:
- `npm test`
- `npm run lint`
- `npm run build`
- `git diff --check`

- [x] **Step 3: Review diff**

Inspect `git diff -- mobile/src tests/unit docs/superpowers/plans/2026-06-18-mobile-app-issue-023-024.md` and confirm no unrelated file changes are included.

- [x] **Step 4: Commit on local prd branch**

Commit only the relevant files with message `fix: guard mobile send and android back listener`.
