# PC 团队过程折叠与成员状态

任务 task-581652e0 / step-9cf8b411；基线 prd fd30c0e。

## 边界

复用 ConversationPane/MessageList/TurnContentView，以可选 PC 团队模式实现。运行历史默认折叠，折叠外只显示最新执行项，完成保留回复。输入框上方显示活跃成员与本页尚未看见的新回复，点击定位。未查看提示为前端页面状态，不新增跨设备未读语义。不改后端、DB、普通 Workspace 或 APP 页面，不重启服务。

## 清单

- [ ] A：补紧凑过程和成员状态回归测试，验证普通默认行为。
- [ ] B：可选过程显示模式、成员状态/可见消息记录、虚拟列表定位。
- [ ] C：浏览器验证并发更新、展开、done、离屏未读和长列表定位。
- [ ] D：npm test/lint/build、UI/mobile 类型检查；更新架构和README。
- [ ] E：独立审查修复、提交、合并prd并核对WIP和合并树。
