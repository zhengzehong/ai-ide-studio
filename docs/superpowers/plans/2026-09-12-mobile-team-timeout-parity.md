# APP 团队超时修复与普通会话样式对齐

任务 task-4b218858 / step-59d9e56f。基线 prd 20c642c。

## 范围与约束

- 修复实时队列背压期间查询结果被丢弃和团队等待恢复相互阻塞。
- 降低团队成员完成后的历史刷新量，保持已完成内容和权限状态。
- 团队详情按普通 APP 对齐，按用户明确“不提取”保留独立页面及团队专属样式模块，不提取共享布局，不修改普通 ChatPage。
- 不重启 PRD，不修改用户 WIP，不新增 DB 表或迁移。
- 独立 worktree 开发，完整验证和审查通过后才合并 prd。

## 执行清单

- [x] A：先补队列恢复、超大响应及控制结果不丢失的复现测试；修正有界发送契约及日志。
- [x] B：补团队恢复与按成员增量刷新测试；复用后端轻量恢复查询，避免全队原始事件回放。
- [x] C：保留独立团队布局，按普通页样式和操作对齐；浏览器验证输入CSS一致、320/390px无横向溢出、置顶未读、流式完成及慢成员恢复窗口。
- [x] D：npm test / lint / build / server+UI+mobile 类型检查、隔离弱网/浏览器验证、diff 检查。
- [x] E：独立审查通过；完成最终检查后合并 prd，实际合并提交及内容复核记录写入任务最终报告。

## 验收重点

恢复查询必须有结果或明确错误；缓存内容不因后台刷新失败而消失；成员完成不得整队重复恢复；工具仍在成员单轮回复内；普通会话及 PC Workspace 行为不回归；手机后台恢复、滚动、输入/权限/工具详情可用。

## 验证记录

- 最终全量 Vitest：463 文件、2565 用例通过；lint、build、server/UI/mobile 有效 tsconfig 类型检查均 EXIT=0。一次并发复跑在 model-proxy-runtime 用例清理临时目录时报 Windows EPERM，单独重跑及降低并发后的全量均通过，未跳过或修改该用例。
- 浏览器隔离验证：320px/390px 无横向溢出；普通与团队输入框字体、字号、行高、内边距、高度一致；置顶/取消置顶、标未读返回、断网禁用和提示通过。
- 实际 UI 与数据控制器配合模拟 RPC：流式完成后内容保留且仅刷新对应成员；恢复先 resume 再查询；慢成员加载期间其他成员新增内容保留且不重复。
- 队列及真实 DB 集成测试验证恢复期间 RPC 响应、权限隔离及快照查询。未对真实 Android 设备和线上故障网络做实测，不声称完全覆盖所有网络条件。
- 普通 mobile ChatPage 与 PC Workspace 无代码改动；共用 WS 查询和队列有变动，已纳入普通会话相关回归测试。
- 构建仅有既有 chunk-size 提示；当前服务未重启。安装中的旧 APK 不随代码合并自动更新。
- 独立审查发现并确认修复：先 resume 后恢复快照，避免多成员恢复窗口丢新事件；Hub 两处恢复通知入队失败均消费 closeRecommended 并以 1013 关闭连接，避免控制回复占满队列后永久停流。慢 socket/满队列复现用例先失败，修复后通过。审查方定向 8 文件 48 用例通过，无剩余阻塞项。
