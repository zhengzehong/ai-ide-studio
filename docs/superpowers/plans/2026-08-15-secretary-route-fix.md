# 项目秘书路由修复

## 目标

修复点击项目秘书入口后被回退到工作对话的问题，不改变秘书业务、Session 或服务端行为。

## 改动

- 将 `/secretary` 纳入项目内路由白名单。
- 增加路由构建与规范化回归测试，确保 `/p/:projectId/secretary` 不再回退到 `/workspace`。

## 验收

- 项目导航点击秘书后进入 `Secretary` 页面。
- 既有项目路由测试、lint、构建通过。
