# 团队列表交互补齐

- [x] 为项目团队列表补缓存、重叠请求去重及跨项目响应隔离测试。
- [x] 补左侧团队缓存，已有缓存时后台刷新，失败保留内容。
- [x] 团队会话改用平台右键菜单、重命名和危险操作确认弹窗；静默刷新不禁用新增。
- [x] 回归测试、构建、lint、审查并提交。
- [ ] 连同 d04edc3 合并 PRD，验证提交可达。

范围：不处理 502，不重启服务，不在主仓库构建或替换在线产物，不修改生产 DB。

离线浏览器验收：`node scripts/verify-team-menu.mjs`，请求由页面内 mock 接管，站点由 Playwright route.fulfill 返回，不启动 HTTP 服务。覆盖缓存展示、右键、改名、取消删除、删除最后一条清空选择，无浏览器异常。截图与 bundle 留在忽略目录 `.tmp/`。

验证结果：444 个测试文件 / 2458 项通过；build、lint、diff --check 通过。审查修正了选择缓存尚未写入时删除后残留标题的问题；删除成功但后续刷新失败仍保留正确的本地删除结果。构建仅有既有的大 chunk 提示。
