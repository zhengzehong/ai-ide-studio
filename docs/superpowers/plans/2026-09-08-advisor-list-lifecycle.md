# 建议列表生命周期实施

目标：忽略立即隐藏、创建24小时后隐藏、当前项目全部忽略。保留反馈和已建任务台账；不重启PRD、不改在线数据。

- [x] 核查当前代码、创建隔离工作树。
- [x] 先补历史七天数据、24小时边界、忽略并发与批量范围复现测试（先确认旧代码失败）。
- [x] 实现建议查询及批量RPC，保留反馈查询、保护派发占用。
- [x] 实现前端批量操作、隐藏、到期刷新与响应竞态保护。定向6文件47用例通过。
- [x] 更新协议及使用文档，定向/全量测试、类型检查、lint/build。
- [x] 审查差异、项目隔离、派发互斥、历史期限兼容、台账保留和响应顺序；未发现阻断问题。

验证：全量423文件2264用例通过；tsc、lint、build、diff --check通过。Playwright在1280/360宽度挂载真实组件（模拟RPC、不启服务），单条忽略、全部忽略、空态禁用、无溢出及无浏览器错误通过。

提交后合并prd并核对包含关系，最终commit及merge结果记录在任务交付报告中。

验收：旧建议无需重新导入；单条/批量忽略刷新不复现；新到建议不被误忽略；过期不可新派发；既有任务及其产物不受影响；已处理展示同样受创建24小时窗口限制。
